Genera una consulta SQL SQLite (solo SELECT) a partir del plan y el esquema.

Pregunta:
{question}

Plan (JSON):
{plan}

Esquema relevante:
{schema}

Reglas:
- Solo SELECT
- Usar tablas/columnas existentes del esquema
- Incluir LIMIT {limit}
- Evitar SELECT *
- Evitar columnas PII (IdPaciente, NombrePaciente, TipoDocumento, MotivoConsulta) salvo agregaciones anónimas
- No inventar tablas ni columnas
- Para UCI: filtrar NombreGrupoCama / NombreSubgrupoCama / NombreCama con LIKE '%UCI%'
- No hay tabla de stock físico: inventario de medicamentos se estima con consumo en MedicamentoInsumo
- Fechas en formato ISO en los datos (YYYY-MM-DD HH:MM:SS)

Devuelve SOLO JSON:
{{
  "intent": "...",
  "sql": "SELECT ...",
  "parameters": {{}},
  "tables": ["..."],
  "columns": ["..."],
  "explanation": "..."
}}
